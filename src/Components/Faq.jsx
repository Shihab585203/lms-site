import React from 'react';
import useTitle from '../useTitle/useTitle';

const Faq = () => {
    useTitle('FAQ');
    return (
        <div>
            faq
        </div>
    );
};

export default Faq;