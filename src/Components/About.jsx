import React from 'react';
import useTitle from '../useTitle/useTitle';

const About = () => {
    useTitle('ABOUT');
    return (
        <div>
            about
        </div>
    );
};

export default About;